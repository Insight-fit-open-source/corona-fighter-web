import styled from 'styled-components';

export default styled.div`
  svg {
    width: auto;
    height: auto;
    max-width: 100%;
    display: inline-block;
    position: relative;
    margin: 0 auto;
  }
  
  &:hover {
    cursor: pointer;
  }
`;
