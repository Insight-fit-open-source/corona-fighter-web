import colors from './colors';

const palette = {
  primary: {
    main: colors.pink,
  },
  secondary: {
    main: colors.orange,
  },
  background: {
    default: colors.white,
  },
};

export default palette;
