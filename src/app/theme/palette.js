import colors from './colors';

const palette = {
  primary: {
    main: colors.blueDark,
  },
  secondary: {
    main: colors.green,
  },
  background: {
    default: colors.white,
  },
};

export default palette;
